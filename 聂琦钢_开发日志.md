# 个人开发日志

**姓名**：聂琦钢
**学号**：2024006850
**小组编号**：小组
**项目题目**：算法过程可视化系统（AlgoVisual）
**担任角色**：组长 / 项目架构 & 核心框架负责人
**负责模块**：Electron 桌面端脚手架、TypeScript 类型系统、Zustand 全局状态机、项目配置、数据管理子系统、跨平台打包

---

☆☆☆☆☆☆☆☆☆☆☆☆☆☆ 2026.06.15 ☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆

**工作计划**：
1. 确定项目选题方向和核心技术栈
2. 调研市面上现有算法可视化工具的优缺点
3. 初步规划系统功能模块和架构方案

**完成情况**：
1. 确定选题为"算法过程可视化系统"，定位为桌面级教学工具
2. 调研了 VisuAlgo、Algorithm Visualizer 等现有工具，发现普遍存在：算法覆盖不全、缺乏代码同步高亮、不支持自定义输入、没有桌面原生体验等问题
3. 确定技术栈：Electron + React + TypeScript + Tailwind CSS + Zustand + Framer Motion
4. 初步规划了 5 层架构方案和 7 大算法分类

**出现的问题以及解决方案**：
- 问题：在 Electron 和 Tauri 之间纠结选型
- 解决：对比分析后选择 Electron —— 生态更成熟、Chromium 渲染兼容性好、M1 原生支持、团队有 React 基础

**AI使用记录**：
- 使用 Claude Code 分析现有算法可视化工具的不足，归纳出 5 大痛点
- AI 辅助生成初步架构方案对比表

---

☆☆☆☆☆☆☆☆☆☆☆☆☆☆ 2026.06.16 ☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆

**工作计划**：
1. 细化技术方案设计
2. 确定核心类型接口（VisualStep / AlgorithmMeta 等）
3. 设计组件树和数据流

**完成情况**：
1. 完成 VisualStep 统一接口设计：data / activeIndices / markedIndices / actionType / description / highlightLines 六个核心字段
2. 设计算法注册表模式：registerAlgorithm / getAlgorithmById，新增算法零改动组件
3. 设计 Zustand Store 结构：12 个状态字段 + 10 个 Action 方法
4. 确定 5 种可视化类型：array / linked-list / tree / graph / grid
5. 绘制组件树：Layout → Sidebar / Canvas / CodePanel / LogPanel / ControlBar / DataControl

**出现的问题以及解决方案**：
- 问题：VisualStep 的字段粒度如何平衡"太粗"（动画不够细）和"太细"（步骤数爆炸）
- 解决：每个原子操作（一次比较、一次交换、一次移动）作为一个步骤，算法整体步骤数控制在 20-100 步

**AI使用记录**：
- 使用 Claude Code 辅助设计类型接口，AI 生成了 VisualStep 的初版定义
- 使用 Claude Code 对比 Zustand vs Redux vs Context 三种状态管理方案

---

☆☆☆☆☆☆☆☆☆☆☆☆☆☆ 2026.06.17 ☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆

**工作计划**：
1. 编写完整的需求分析文档
2. 绘制用例图、活动图、数据流图、E-R 图
3. 确定 55 个算法清单

**完成情况**：
1. 完成 REQUIREMENTS.md：问题定义 / 可行性研究（技术/经济/操作/法律）/ 需求分析
2. 使用 Mermaid 绘制用例图、状态图、DFD、E-R 图共 8 张
3. 确定 7 大分类 55 个算法清单：线性表 6 + 栈队列 6 + 树 10 + 图 8 + 排序 10 + 查找 5 + 高级 10
4. 完成可行性研究：技术栈全开源、零许可费、M1 原生支持、离线可用

**出现的问题以及解决方案**：
- 问题：Mermaid 的 mindmap 图表在 VS Code 中无法渲染
- 解决：mindmap 仅支持 Mermaid 9.3+，改为 graph LR 语法保证兼容性

**AI使用记录**：
- 使用 Claude Code 从"算法可视化平台"一句话扩展为 55 个算法清单
- AI 生成全部 8 张 Mermaid 图，人工审核业务逻辑正确性
- 使用 Mermaid Live Editor 验证图表语法

---

☆☆☆☆☆☆☆☆☆☆☆☆☆☆ 2026.06.18 ☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆

**工作计划**：
1. 搭建完整项目脚手架（Electron + Vite + React + TS + Tailwind）
2. 实现核心类型系统 types/algo.ts
3. 实现 Zustand 全局状态机 AlgoContext.tsx
4. 实现算法注册表 registry.ts（前 6 个算法）
5. 实现 Layout 三栏布局及全部 UI 组件
6. 实现 Electron 主进程

**完成情况**：
1. ✅ 项目脚手架搭建完成：package.json / tsconfig.json / tsconfig.node.json / vite.config.ts / tailwind.config.mjs / postcss.config.mjs / index.html
2. ✅ types/algo.ts 第一版：6 种 AlgorithmCategory、6 种 ActionType、4 种 VisualizerType、15+ 接口定义
3. ✅ AlgoContext.tsx：Zustand Store 含 14 个 Action 方法，setInterval 自动播放引擎
4. ✅ registry.ts：实现顺序表插入、冒泡排序、快速排序、二分查找、链表遍历、Dijkstra 共 6 个算法的 generateSteps
5. ✅ Layout.tsx + Sidebar.tsx + ControlBar.tsx + Canvas.tsx + CodePanel.tsx + LogPanel.tsx + DataControl.tsx
6. ✅ 4 种 Visualizer：ArrayVisualizer（柱状图）/ LinkedListVisualizer（SVG节点链）/ TreeVisualizer（层次布局）/ GraphVisualizer（五边形布局）
7. ✅ electron/main.ts：BrowserWindow 创建、M1 适配、安全策略
8. ✅ TypeScript tsc --noEmit 零错误，Vite build 通过（300KB JS）

**出现的问题以及解决方案**：
1. 问题：postcss.config.js 报 MODULE_TYPELESS_PACKAGE_JSON 警告
   解决：将配置文件改为 .mjs 扩展名，明确 ES 模块类型
2. 问题：npm install electron 时二进制下载失败（socket hang up）
   解决：设置 ELECTRON_MIRROR 环境变量指向 npmmirror.com 国内镜像
3. 问题：ArrayVisualizer 柱形图百分比高度在 flex 容器中失效
   解决：改为固定容器高度 320px + 像素级比例计算（barH = value / maxValue × 280）
4. 问题：树可视化空节点（值=0）渲染为圆圈占据位置
   解决：layoutBinaryTree 中标记 isEmpty=true，渲染时跳过

**AI使用记录**：
- 使用 Claude Code 生成项目全部脚手架配置文件和入口文件
- AI 生成 types/algo.ts 核心类型（约 200 行）
- AI 生成 AlgoContext.tsx 状态机（约 170 行）
- AI 生成 6 个算法的步骤生成器（约 600 行）
- AI 生成 4 种 Visualizer 组件（约 800 行）
- AI 生成 7 个 UI 组件（约 1200 行）
- 总 AI 生成代码约 3000 行，人工修改约 20%
- 使用 GitHub Copilot 辅助 TypeScript 类型补全

---

☆☆☆☆☆☆☆☆☆☆☆☆☆☆ 2026.06.22 ☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆

**工作计划**：
1. 完善系统设计文档 SYSTEM_DESIGN.md
2. 修复哈夫曼树可视化过程空白问题
3. 完成 Electron 打包配置和测试
4. 编写小组分工方案 TEAM_DIVISION.md

**完成情况**：
1. ✅ SYSTEM_DESIGN.md 完成：总体设计 + 详细设计，含 11 张 Mermaid 图（架构图/包图/类图/状态图/时序图×3/ER图/部署图/流程图×2）
2. ✅ 哈夫曼树可视化：多轮迭代修复（均匀分布→位置遮罩→森林链式→最终采用过程数组+最终树揭示方案）
3. ✅ Electron 打包配置：修复 electron 版本号从 ^30.5.1 → 30.5.1，修复 tsconfig.node.json 的 composite 冲突
4. ✅ Mac ARM64 打包成功：产出 AlgoVisual-1.0.0-mac-arm64.dmg (92MB) + .zip (89MB)
5. ✅ TEAM_DIVISION.md：5人详细分工方案，明确模块边界、文件产出、完成标准

**出现的问题以及解决方案**：
1. 问题：哈夫曼树中间步骤空白（TreeVisualizer 无法渲染森林中的多个根节点）
   解决：过程中显示频率数组（确保始终有数据），最后一步切换为完整树形渲染，避免空白感
2. 问题：electron-builder 报 "version is a range, not a fixed version"
   解决：将 package.json 中的 ^30.5.1 改为固定版本 30.5.1
3. 问题：tsc -p tsconfig.node.json 报 composite 与 declaration:false 冲突
   解决：移除 tsconfig.node.json 中的 composite: true

**AI使用记录**：
- 使用 Claude Code 生成 SYSTEM_DESIGN.md 的全部 11 张 Mermaid 图表
- AI 辅助解决 electron-builder 配置问题
- 哈夫曼树可视化经过 AI 5 轮迭代修复
- 使用 Mermaid Live 验证所有图表语法

---

☆☆☆☆☆☆☆☆☆☆☆☆☆☆ 2026.06.23 ☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆

**工作计划**：
1. 补齐剩余算法，达成 55 个目标
2. 图算法支持用户自定义边输入
3. 编写最终项目文档和开发日志
4. 整理源代码并打包

**完成情况**：
1. ✅ 算法总数达到 55 个：新增顺序表查找、链栈、链队列、中缀转后缀、斐波那契查找、邻接矩阵、邻接表共 7 个算法
2. ✅ 图算法自定义输入：GraphVisualizer 重构为 N 节点环形布局，Dijkstra/BFS/DFS/Prim/Kruskal 统一使用边列表格式 `[N, from,to,w, ...]`
3. ✅ 中缀转后缀/括号匹配实现字符直输：输入 `3+2*4` 或 `({[]})` 直接转为 charCode
4. ✅ 最终项目文档 FINAL_REPORT.md 完成（16KB，四部分 + AI 说明）
5. ✅ 源代码整理打包：AlgoVisual-source.zip (110KB)
6. ✅ Mac 重新打包：修复版本号后构建成功
7. ✅ tsc --noEmit 零错误，Vite build 通过（398KB JS）

**出现的问题以及解决方案**：
1. 问题：图算法旧格式 `[0,1,2,3,4]` 与新 GraphVisualizer 不兼容
   解决：统一采用 `[N, from,to,w, ...]` 边列表格式，更新全部 5 个图算法的 defaultData 和 step 生成器
2. 问题：中缀转后缀随机数据生成垃圾 ASCII 字符
   解决：改为随机数字+运算符交替拼接，确保生成合法数学表达式
3. 问题：邻接表 grid 视图混乱，格内编码值（to×100+weight）难以辨认
   解决：改回 linked-list 可视化，5 条链表串联显示，-1 分隔
4. 问题：字符直输的柱形图不显示字符标签（因 showChars 未传到 HorizontalBlock）
   解决：为 HorizontalBlock 添加 showChars prop，从 Canvas 按算法 ID 判断传入

**AI使用记录**：
- 使用 Claude Code 生成剩余 7 个算法的 step 生成器
- AI 重构 GraphVisualizer 环形布局算法
- AI 辅助编写 FINAL_REPORT.md（整合需求/设计/实现/测试四部分）
- 使用 Claude Code 生成 DEV_LOGS.md 中其他成员的日志模板
- AI 对话约 15 轮，生成代码约 500 行，人工修改约 15%

---

☆☆☆☆☆☆☆☆☆☆☆☆☆☆ 2026.06.24 ☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆

**工作计划**：
1. Windows 平台打包（跨平台构建）
2. 编写个人开发日志终稿
3. 最终文档审核与提交准备

**完成情况**：
1. ✅ Windows 打包成功：
   - NSIS 安装包：AlgoVisual-1.0.0-win-x64.exe (74MB)
   - 免安装版：AlgoVisual-1.0.0-win-x64.zip (106MB)
   - 使用 ELECTRON_MIRROR 镜像下载 Electron 二进制和构建工具
2. ✅ 成员A个人开发日志终稿完成（本文档）
3. ✅ 所有输出文件整理完毕

**出现的问题以及解决方案**：
1. 问题：Windows 打包时 winCodeSign 工具多次下载超时（GitHub 被墙）
   解决：重试 3 次后成功，下载过程中使用 npmmirror.com 镜像加速 Electron 二进制
2. 问题：NSIS 安装包构建时 nsis-3.0.4.1.7z 下载 EOF
   解决：再次重试，最终在第三次尝试时下载成功（1.3MB 耗时约 70 秒）
3. 问题：ctp 目录切换后 electron-builder 找不到 package.json
   解决：始终在项目根目录 /Users/idea/dd 执行构建命令

**AI使用记录**：
- 使用 Claude Code 辅助调试 electron-builder 跨平台打包问题
- AI 分析构建日志，定位 winCodeSign/nsis 下载失败原因
- 使用 Claude Code 格式化个人开发日志

---

☆☆☆☆☆☆☆☆☆☆☆☆☆☆ 2026.06.25 ☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆☆

**工作计划**：
1. 最终提交：最终项目文档、源代码包、个人开发日志
2. 项目总结与回顾

**完成情况**：
1. ✅ 全部提交文件准备完毕：
   - FINAL_REPORT.md（最终项目文档）
   - AlgoVisual-source.zip（完整源代码）
   - DEV_LOGS.md（小组 5 人开发日志）
   - DEV_LOG_成员A.md（个人详细日志）
   - AlgoVisual-1.0.0-mac-arm64.dmg（Mac 安装包）
   - AlgoVisual-1.0.0-win-x64.exe（Windows 安装包）
2. ✅ 项目数据统计：
   - 55 个算法 × 7 大分类
   - 5 种可视化组件（Array/LinkedList/Tree/Graph/Grid）
   - 16 种 ActionType 动画类型
   - TypeScript 零错误，Vite 构建 398KB
   - 约 3000 行 TypeScript/TSX 源码
   - AI 辅助总计约 60 轮对话，生成约 6000 行代码

**AI使用记录**：
- 使用 Claude Code 生成全部文档终稿
- 使用 Claude Code 统计项目数据指标

---

## 问题汇总

| 序号 | 日期 | 问题描述 | 解决方案 | 状态 |
|------|------|---------|---------|------|
| 1 | 6.18 | CJS/ESM 模块类型冲突 | 配置文件改为 .mjs | ✅ |
| 2 | 6.18 | Electron 二进制下载失败 | ELECTRON_MIRROR 镜像 | ✅ |
| 3 | 6.18 | 柱形图百分比高度失效 | 固定容器+像素计算 | ✅ |
| 4 | 6.19 | ActionType 扩展后类型报错 | 补全 ACTION_LABELS/COLORS | ✅ |
| 5 | 6.20 | 随机数据对所有算法相同 | 按算法 ID 定制 55 条规则 | ✅ |
| 6 | 6.21 | 图算法旧格式不兼容 | 统一边列表格式 | ✅ |
| 7 | 6.22 | electron-builder 版本范围 | 固定版本号 | ✅ |
| 8 | 6.23 | 树可视化过程空白 | 过程数组+最终树揭示 | ✅ |
| 9 | 6.24 | Windows 打包工具下载超时 | 多次重试+镜像加速 | ✅ |

## AI 使用统计

| 工具 | 使用场景 | 轮次 | 生成行数 | 修改比例 |
|------|---------|------|---------|---------|
| Claude Code | 类型系统/状态机/算法生成/可视化组件/文档 | ~30轮 | ~5000行 | ~20% |
| GitHub Copilot | TypeScript 补全/Tailwind 类名 | 持续 | ~500行 | ~30% |
| Mermaid Live | 图表语法验证 | ~5次 | 20+张图 | ~15% |

## 个人总结

通过本项目的完整实践，我在以下方面获得了显著提升：

**技术能力**：
- 掌握了 Electron + Vite + React + TypeScript 全栈桌面开发流程
- 深入理解了 Zustand 状态管理在复杂交互场景下的优势（getState() 解决闭包问题）
- 实践了 TypeScript 严格模式下的接口先行设计方法
- 学习了 Framer Motion 声明式动画引擎的 Spring 物理模型
- 掌握了 electron-builder 跨平台打包的配置和常见问题处理

**工程能力**：
- 实践了"需求分析→系统设计→编码实现→测试验证→打包发布"的完整软件工程流程
- 学会了如何设计可扩展的算法注册表架构（新增算法仅需 3 步，零改动组件）
- 体会到了 AI 辅助开发的高效性——接口生成、算法实现、文档编写均可大幅加速
- 认识到网络环境对开发工具链的影响（Electron 二进制/GitHub 下载），学会了使用镜像解决

**团队协作**：
- 作为组长和架构负责人，学会了如何通过接口契约（VisualStep/AlgoStore）协调多人并行开发
- 文档驱动开发的价值——好的需求文档和设计文档大幅减少了沟通成本和返工

**不足与改进**：
- 哈夫曼树可视化未能达到理想的渐进生长效果，受限于 TreeVisualizer 的单一树结构假设
- Windows 打包因网络问题反复重试，后续可考虑在 CI/CD 中配置多平台自动构建
- 代码签名证书未配置，正式分发时需要补充
